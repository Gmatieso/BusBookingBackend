class ApplicationRecord < ActiveRecord::Base
  #Inheriting from ActiveRecord::Base  class which means that it access to a number of method  that assist in working with the database 
  primary_abstract_class
end
